[project]
name = "jarvis-adapter-connector-mcp"
version = "0.1.0"
description = "Adapter implementing a jarvis_core Port using mcp"
requires-python = ">=3.12"
dependencies = [
    "jarvis-core",
    "mcp",
]

[tool.uv.sources]
jarvis-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
