[project]
name = "jarvis-adapter-capability-store-sqlite"
version = "0.1.0"
description = "Adapter implementing a jarvis_core Port using sqlmodel"
requires-python = ">=3.12"
dependencies = [
    "jarvis-core",
    "sqlmodel",
]

[tool.uv.sources]
jarvis-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
