[project]
name = "jarvis-shared"
version = "0.1.0"
description = "Framework-agnostic cross-cutting utilities (logging, common types). No business logic."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
